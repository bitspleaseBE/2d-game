{
  "name": "wandertrap-game",
  "version": "1.0.0",
  "description": "Cool 2d platform game for bedtime",
  "scripts": {
    "start": "parcel index.html --no-cache",
    "build": "parcel build index.html --public-url ./ --no-cache",
    "test": "playwright test",
    "narrate:story": "node scripts/generate-narration.js"
  },
  "author": "Emiel Masyn",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "parcel": "^2.12.0"
  }
}
